<!--
  Merge these into android/app/src/main/AndroidManifest.xml after running
  `react-native init`. SEND_SMS is only needed if you wire up the optional
  SilentSmsModule.kt path — the default JS flow (Linking to the native SMS
  composer) needs no SMS permission at all.
-->
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
<!-- Used by @react-native-voice/voice's Android SpeechRecognizer for Speech Check -->
<uses-permission android:name="android.permission.RECORD_AUDIO" />

<!-- Optional: only if you enable the silent-SMS reference module -->
<uses-permission android:name="android.permission.SEND_SMS" />

<!-- Declares camera+flash hardware use for VisionCamera PPG capture -->
<uses-feature android:name="android.hardware.camera" android:required="true" />
<uses-feature android:name="android.hardware.camera.flash" android:required="true" />

<!-- Front camera is used by Face Check; not required="true" since a small
     number of devices lack one — the app should just disable that one
     feature rather than fail to install. -->
<uses-feature android:name="android.hardware.camera.front" android:required="false" />
<uses-feature android:name="android.hardware.microphone" android:required="false" />
